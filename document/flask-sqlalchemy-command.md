
```
# Mapping already exist database and than create a model.py for dateabase tables.
flask-sqlacodegen --outfile models.py --flask postgresql://admin:1qaz2wsx@localhost/

# run flask server.
python manage.py runserver
```